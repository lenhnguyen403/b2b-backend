server:
  port: 8081

spring:
  application:
    name: account-service

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:userb2b
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true